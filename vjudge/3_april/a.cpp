#include <iostream>
#include <cstdio>
using namespace std;

int main()
{
    int n,m;
    int c;

    scanf("%d %d", &n, &m);
    for (int i = 0; i < n; i++)
    {
        cin >> buffer;
        j = 0;
        c = 0;
        while (buffer[j])
        {
            if (buffer[j] == '4' || buffer[j] == '7')
                c++;
            j++;
        }
        if (c <= k)
            res++;
    }
    cout << res << '\n';
    return 0;
}