#include <cstdio>
#include <vectors>

int main()
{
	int num_of_test;
    int n;

	scanf("%d", &num_of_test);
	for (int i = 1; i <= num_of_test; i++)
	{
		scanf("%d", &n);
        vector<int> 
		low++;
		scanf("%d", &tries);
		while (true)
		{
			med = (low + hight) / 2;
			printf("%d\n", med);
			fflush(stdout);
			scanf("%s", s);
			if (s[0] == 'C') break;
			if (s[4] == 'S') low = med + 1;
			else hight = med - 1;
		}
	}
	return 0;
}
